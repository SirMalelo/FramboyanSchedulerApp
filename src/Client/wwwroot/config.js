window.appConfig = {
    apiBaseUrl: '#{API_BASE_URL}#', // Will be replaced during deployment
    environment: 'production',
    features: {
        enableDebugMode: false,
        enablePerformanceLogging: true
    }
};
